{
	"name": "typescript-config",
	"version": "0.0.1",
	"license": "MIT",
	"private": true,
	"scripts": {
		"format": "prettier --check . --ignore-path=../../.gitignore --cache --cache-location=../.cache/.prettier-cache-tsconfig",
		"format:fix": "prettier --write . --ignore-path=../../.gitignore --cache --cache-location=../.cache/.prettier-cache-tsconfig"
	}
}
